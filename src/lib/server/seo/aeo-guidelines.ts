// src/lib/server/seo/aeo-guidelines.ts
// Shared AEO content rules appended to system prompts of AI features that
// produce lead-facing or admin-facing text (lead generator, CRM insights).

export const AEO_CONTENT_GUIDELINES = `
Content trust rules (AEO):
- Never state a yield/ROI figure without labelling its status (actual result vs forecast) and its date.
- No bare promises like "up to 25%" — a figure needs assumptions or must be omitted.
- Every market statistic needs a source and a year.
- Forecast figures must carry a disclaimer that returns are not guaranteed.
- Write in one language per message; do not mix Russian and English.`;
